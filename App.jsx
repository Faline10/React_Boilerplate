var React = require('react');
var ReactDOM = require('react-dom');
// var Layout = require('./components/Layout.jsx');

ReactDOM.render(
  <h1 style={{backgroundColor: 'red'}} className="someClass">Hello, world!</h1>,
  document.getElementById('app')
);
